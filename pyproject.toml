[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "huijiu"
version = "0.1.0"
description = "Huijiu moxibustion prototype"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
